<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PawrkingSample.Pages.SeeAllLotsPage"
             Title="Parking Lots Available"
             NavigationPage.HasBackButton="False">
    <ContentPage.Content>
        <StackLayout Spacing="10" >
            <Label/>
            <Image 
                    Source="PawParkingLog.png"
                     />
            <Label/>
            <Button
                    x:Name="LotC"
                    Text="Lot C"
                    Command="{Binding LotCButonClicked}"
                    BackgroundColor="White"
                    IsVisible="True"
                    />
            <Label/>
            <Button
                    x:Name="PS1"
                    Text="PS1"
                    Command="{Binding PS1ButtonClicked}"
                    BackgroundColor="White"
                    />
            <Label/>
            <Button
                    x:Name="LotP"
                    Text="Lot P"
                    Command="{Binding LotPButtonClicked}"
                    BackgroundColor="White"
                    />
            <Label/>
            <Button
                    x:Name="Cancel"
                    Text="Cancel"
                    Command="{Binding CancelButtonClicked}"
                    BackgroundColor="White"
                    />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>